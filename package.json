{
  "name": "yt-downloader-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": { "node": ">=18.0.0" },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5"
  }
}

{
  "scripts": {
    "start": "node server.js",
    "postinstall": "npm install -g yt-dlp@latest && yt-dlp -U"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
